#include <bits/stdc++.h>
using namespace std;
void possibleN(int n, int x, int cnt)
{
    if (x == n)
        return;
    for (int i)
}
int main()
{
    int n;
    cin >> n;
    int cnt = 0;
    possibleN(n, 1, cnt);
    cout << cnt << endl;
    return 0;
}